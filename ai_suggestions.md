# AI Suggestions

- Builder workflow would benefit from `bfind`, compact zone inventories, and `contentcheck` before the next broad authored-content push.
- Help authoring should grow into a builder-facing `hedit` workflow with publish/incomplete state once the current command-help coverage is stable.
